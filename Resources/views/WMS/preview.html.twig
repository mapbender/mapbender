{% extends '::base.html.twig' %} 

{% block title %}WMS Preview{% endblock %}

{% block body %}
    <p>WMS Preview '{{ getcapa_url }}'</p>
    <div class="wms">
        <h1 class="title">{{ wms.getTitle() }}</h1>
        <h2 class="name">{{ wms.getName() }}</h2>
        <p class="abstract">{{ wms.getAbstract() }}</p>
        <p class="address">
        </p>
        <h3>Layer:</h3>
        <ul class="layers">
            <li class="layer">
                <h1 class="title">Neuer Layer</h1>
                <h2 class="name">layer_ney</h2>
            </li>
        </ul>
    </div>
    <form action="{{ path('wms_add') }}" method="POST">
        <input type="submit" name="save" value="WMS hinzufügen"/> 
    </form>
{% endblock %}
