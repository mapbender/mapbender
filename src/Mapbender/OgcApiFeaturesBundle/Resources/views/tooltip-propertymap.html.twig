{% for key, value in properties %}
    <div class="ogc-api-tooltip-row">
        <span class="ogc-api-tooltip-key">{{ value | trans }}</span>
        <span class="ogc-api-tooltip-val">{{ '${' }}{{ key }}{{ '}' }}</span>
    </div>
{% endfor %}
