<div class="mb-element mb-element-searchrouter" id="{{ id }}" data-title="{{ title | trans }}">
    {{ form_row(route_select_form, {'label': false}) }}

    <div class="search-forms">
        {% for form in search_forms %}
            <form name="{{ form.vars.name }}" autocomplete="off">
                {{ form_widget(form) }}
            </form>
        {% endfor %}
    </div>
    <div class="search-action-buttons">
        <span data-action="reset" class="btn btn-sm btn-default">{{ 'mb.actions.reset' | trans }}</span>
        <span data-action="search" class="btn btn-sm btn-default">{{ 'mb.actions.search' | trans }}</span>
    </div>
    <div class="search-results"></div>
</div>
