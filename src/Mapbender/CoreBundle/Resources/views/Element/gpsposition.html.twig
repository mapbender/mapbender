<span id="{{ id }}" class="mb-gpsButton iconBig {{ configuration.icon|default('iconGPS') }}" title="{{ configuration.tooltip|default(title)|trans }}">{% if configuration.label %}{{ title|trans }}{% endif %}</span>
