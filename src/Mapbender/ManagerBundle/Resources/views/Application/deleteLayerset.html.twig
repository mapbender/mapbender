{# base message contains <i> tags to italicize the title, so escaping is a little more involved here... #}
{{ "mb.manager.admin.layerset.delete.confirm" | trans({'%layerset_title%': layerset.title | e}) | raw }}
