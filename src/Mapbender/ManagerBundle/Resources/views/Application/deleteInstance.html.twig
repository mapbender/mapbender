{% block manager_content %}

{{ form_start(form, {"attr": {"action": path('mapbender_manager_application_deleteinstance', {'slug': application.slug, 'layersetId': layerset.id, 'instanceId': instance.id})}}) }}
    {% trans with {'%instance_title%': instance.title} %} mb.manager.admin.instance.delete.confirm {% endtrans %}
    {{ form_widget(form) }}
{{ form_end(form) }}

{% endblock %}