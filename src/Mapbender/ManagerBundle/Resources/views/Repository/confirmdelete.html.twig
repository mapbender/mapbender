{{- form_start(form) -}}
<p>{{"mb.manager.admin.source.delete"|trans}} {{ source.title }}?</p>

{% include '@MapbenderManager/Repository/source/applications.html.twig' with {'applications': applications } %}
{{- form_end(form) -}}
