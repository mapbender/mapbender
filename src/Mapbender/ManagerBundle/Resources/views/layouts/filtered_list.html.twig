{% extends "MapbenderManagerBundle::manager.html.twig" %}

{% block manager_content %}
    {%- set list_id = list_id | default('listFilter0') -%}
    <div class="list-head">
        {%- if items -%}
            {% include "MapbenderManagerBundle:Element:filter-container.html.twig" with {filterTargetId: list_id} only %}
        {%- endif -%}
        {%- block list_actions -%}{%- endblock -%}
    </div>
{% endblock %}
