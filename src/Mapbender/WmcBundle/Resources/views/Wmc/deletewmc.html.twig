{% set wmc_title = wmc.state.title %}
{% set wmc_id = wmc.id %}

{{ form_start(form, { 'action': path('mapbender_core_application_element', { 'slug': application.slug, 'id': id, 'action': 'delete' }), 'method': 'POST', 'attr': {
    novalidate: 'novalidate', autocomplete: 'off', id: 'wmc-delete'
}}) }}
    <header>
    {{ 'mb.wmc.wmc.delete_confirm' | trans ({'%wmc_title%': wmc_title,'%wmc_id%': wmc_id}) | raw }}
    </header>
    {{ form_widget(form) }}
    <input type="submit" class="hidden"/>
{{ form_end(form) }}
