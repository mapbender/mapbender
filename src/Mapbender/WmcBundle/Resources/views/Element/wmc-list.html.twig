{% if 'listloader' in configuration.components %}
<a href="{{ url('mapbender_manager_element_select', { 'slug': application.slug, 'region': region }) }}" class="iconAdd iconSmall addElement" title="{{'Add new wmc to'|trans}} {{region}}"></a>
{% endif %}
{% if 'listloader' in configuration.components %}
<table class="elementsTable tableCore">
    <thead>
        <tr>
            <th>{{ 'Id' | trans }}</th>
            <th class="type">{{ 'Title' | trans }}</th>
            <th class="type">{{ 'Description' | trans }}</th>
            <th class="iconColumn"></th>
        </tr>
    </thead>
    <tbody>
    {% if wmclist|length > 0 %}
      {% for wmc in wmclist %}
        <tr class="element" data-id="{{element.id}}" data-href="{{ path('mapbender_manager_element_weight', {'id': wmc.id})}}">
            <td class="titleColumn description">{{ wmc.id }}</td>
            <td class="typeColumn description">{{ wmc.title }}</td>
            <td class="typeColumn description">{{ wmc.description }}</td>
            <td class="iconColumn">
                <div class="checkWrapper left iconCheckbox iconSmall {% if wmc.public %}iconCheckboxActive{% endif %}" title="{{"Toggle public/private wmc"|trans}} {{wmc.title}}">
                    <input class="checkbox" data-href="{{ path('mapbender_manager_element_enable', { 'id': element.id })}}" data-id="{{ element.id }}" type="checkbox" {% if element.enabled %} checked="checked" {% endif %}>
                </div>
                <span class="iconEdit editElement iconSmall" title="{{"Wmc element"|trans}}  {{wmc.title}}" data-url="{{ path('mapbender_manager_element_edit', {'slug': application.slug, 'id': wmc.id})}}"></span>
                <span class="iconRemove removeElement iconSmall" title="{{"Delete Wmc"|trans}} {{wmc.title}}" data-id="{{ wmc.id }}" data-slug="{{ application.slug }}" data-url="{{ path('mapbender_manager_element_confirmdelete', {'slug': application.slug, 'id': element.id}) }}"></span>
            </td>
        </tr>
      {% endfor %}
        <tr class="placeholderRow"></tr>
    {% else %}
        <tr>
            <td class="description">{{"No wmc added."|trans}}</td>
            <td></td>
            <td></td>
            <td></td>
        </tr>
    {% endif %}
    </tbody>
</table>
{% endif %}